# 20Python-pip
Archivo de las clases del cuarto semestre, juego Piedra, papel o tijera en Python

Este repositorio esta en miPC desde git bash como administrador, creación de las diferentes ramas de trabajo:

```sh
    git clone git@github.com:ArielBetancud22/20Python-pip.git
    cd 20python-pip
    mkdir game
    cd game
    touch main.py
    code . #Carga del algoritmo
    git add .
    git commit -m"Carga del algoritmo"
    git push origin main
    git branch second
    git checkout second
    git push origin second
    git checkout main
    git branch ariel22
    git checkout ariel22
    git push origin ariel22 #Listo para seguir trabajando
```